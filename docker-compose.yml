version: '3.8'

services:
  staging:
    build: .
    container_name: app-staging
    ports:
      - "8081:8080"

  production:
    build: .
    container_name: app-production
    ports:
      - "8082:8080"